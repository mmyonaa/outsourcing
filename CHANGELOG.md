# 변경 이력 (Changelog)

이 프로젝트의 주요 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 참고하며,
배포는 pm2 연속 배포 방식이고 버전은 **논리적 마일스톤**으로 부여합니다(유의적 버전 지향).

> 분류: `추가`(기능) · `개선`(변경) · `수정`(버그) · `보안` · `기타`(빌드/문서/의존성)
> 이 문서 이전의 상세 내역은 git 커밋 기록을 참고하세요.

---

## v1.5.0 — 2026-08-19

전면 코드 리뷰 스프린트 — 백엔드·프론트 전 계층 리뷰 후 버그 60여 건 수정, 보안 보강, UI/UX 개선, 복붙 구조 통합 리팩토링(순감소 약 4,000줄).

### 수정
- **백엔드 데이터 파괴 버그** — 부분 update 시 미전송 필드가 NULL/기본값으로 덮여 **본문이 유실되고 삭제된 글이 되살아나던 문제**(이제 클라이언트가 보낸 필드만 SET), 가상 default 배너가 단건 조회에 끼어들어 **배너 삭제가 항상 실패하며 성공을 반환하던 버그**, dotenv가 DB/S3 설정 캡처보다 늦게 로드되던 import 호이스팅 문제
- 삭제를 SELECT 후 전체 컬럼 되쓰기에서 **단일 UPDATE(soft delete)** 로 교체 — 동시 수정 유실 제거, 배너 totalCount·default 분기 정합
- **상세 페이지 크래시** — 삭제된/잘못된 id로 진입 시 undefined 접근으로 터지던 것을 "찾을 수 없습니다" 안내 화면으로 대체 (공개 4종 + 관리자 편집 4종)
- 목록 복붙 드리프트 계열 — 한 번 실패하면 "다시 시도"가 영영 안 먹히던 error 리셋 누락, 검색 시 같은 요청이 2번 나가던 레이스(응답 순서에 따라 엉뚱한 페이지 표시), 목록 이탈 시 `"undefined"` id로 나가던 유령 요청, 페이지를 넘겨도 1부터 시작하던 목록 번호
- **SEO 회귀** — 하이드레이션 시 전역 useHead가 프리렌더된 페이지별 메타를 `BKTHEATER`/빈 description으로 덮어쓰던 문제(v1.4.1 색인 수정을 무효화하던 원인), canonical/og:url이 이전 페이지를 가리키던 시점 문제, 상세 100건 초과 시 프리렌더가 조용히 누락되던 페이지네이션, www/non-www 혼용, 옛 URL 리디렉션의 잔여 `?id=` 쿼리
- 관리자 — 활성 메뉴 하이라이트 불능·빈 화면이 되던 '대관' 메뉴, 파일 첨부 다이얼로그가 2번 뜨던 중복 바인딩, 썸네일 제거가 저장에 반영되지 않던 문제, 등록/수정/삭제 **실패 시 무반응**(작성 내용 유실 위험 — alert 추가), 배너 저장 성공 오판정
- 기타 — 로고 클릭 시 히스토리 2번 쌓임, 페이지네이션 이전 화살표 비활성 판정, multer 오류가 500으로 응답되던 것(413/400), 한글 첨부 파일명 깨짐(latin1→utf8 복원), 작성일 라벨에 수정일이 표시되던 것, 공간 안내 썸네일이 푸터를 침범하던 라이트박스 레이아웃(릴리스 당일 핫픽스)

### 보안
- 업로드 파일 **확장자 화이트리스트**(html/svg/실행파일 차단, 서버+클라이언트 동일 목록), 이미지 MIME+확장자 이중 검증, S3 키 확장자 정규화
- **조회수 전용 엔드포인트**(`/board/increaseViews`·`/perfo/increaseViews`, 서버 원자적 +1) 신설 — 전체 엔티티 update로 조회수를 보내던 구조(임의 값 조작·경합·본문 덮어쓰기 위험) 폐기, update에서 `views`/`delYn` 수정 차단
- 요청 body 한도 20MB→5MB, ILIKE 검색어 와일드카드 이스케이프, DDL 제약(NOT NULL·CHECK)과 목록 인덱스 보강

### 추가
- 공지·보도 상세 **이전/다음 글 내비게이션**, 섹션 **서브탭**, 포스터 **라이트박스**(확대 보기)
- 오시는 길 보강(주소 복사·길찾기 링크·지도 로드 실패 폴백), 대관 스케줄 모바일 AGENDA 뷰 + 대관 신청 CTA, 배너 업로드 모바일 크롭 가이드

### 개선
- **복붙 통합 리팩토링** — 공개 목록/상세 8개 뷰와 관리자 12개 뷰(동일률 실측 88~99%)를 공용 페이지 10개 + 공용 QuillEditor 컴포넌트로 통합, 기존 뷰는 props만 주입하는 래퍼로 유지(라우트·문구·동작 동일). "한 파일만 고쳐지는" 드리프트 버그의 재발을 구조적으로 차단
- 죽은 코드 대거 제거 — 미사용 인증 배관(로그인 없는 설계), 타 프로젝트 잔재 스토어/컴포넌트, 동작하지 않던 스크롤 저장 로직(→ savedPosition 기반으로 교체, 뒤로가기 위치 복원)
- 접근성 — GNB/서브메뉴 router-link 화(키보드·새 탭·크롤링), 페이지네이션 키보드 접근+aria, 홈 배너 aria-label·호버 일시정지, 모바일 사이드 메뉴 백드롭/ESC/스크롤 잠금
- 명칭 통일(자체/대관 프로그램·대관 신청), 카드 제목 2줄 클램프, PWA 프리캐시 정리(상세 HTML 제외·구버전 캐시 정리), 로컬 개발용 `VITE_API_SERVER` 재정의 지원
- 백엔드 서비스 보일러플레이트 통합(withDbError), 목록+카운트 병렬화, `src/middleware/` 디렉터리 구조 정리, 리포지토리 반환 타입 정정

### 기타
- openapi.yaml에 신규 엔드포인트·부분 수정 명세 반영, 운영 DB 적용용 마이그레이션 스크립트(`ddl/migration-20260818-constraints-indexes.sql`, 적용 완료)
- 리팩토링 전후 동등성 검증(코드 교차검토 + 브라우저 스모크 테스트, 데스크톱/모바일) 수행

---

## v1.4.1 — 2026-07-21

검색 색인 수정 — 상세페이지가 색인되지 않던 문제(Search Console "발견됨/크롤링됨 - 현재 색인이 생성되지 않음")를 프리렌더 복구로 해결.

### 수정
- **상세페이지 색인 불가 문제 해결** — 라우터 가드가 SSR에서 `next()` 없이 return하여 `router.isReady()`가 resolve되지 않아 **vite-ssg 프리렌더가 통째로 스킵**되고 있었음(dist에 `index.html`만 생성 → 모든 URL이 빈 SPA 껍데기 + 홈 기본 메타로 노출). 가드를 `isClient`로 감싸 항상 `next()`를 호출하도록 수정
- **옛 URL(`/{섹션}/detail?id=`) → 새 경로 형식(`/{섹션}/{id}`) 리디렉션** — 옛 형식 URL이 라우터에서 잘못 매칭돼 사용자에게 빈/에러 페이지로 보이고 Search Console에는 "중복/대체 페이지"로 남던 문제 해결. 옛·새 URL의 id가 동일하여 그대로 매핑(nginx 301 + Vue Router 폴백). 대상: `performance`·`performance/next`·`notice`·`news`

### 개선
- 빌드 시 각 URL을 **콘텐츠 + 페이지별 메타가 담긴 정적 HTML로 프리렌더** (정적 라우트 + API에서 수집한 상세 라우트)
- 프리렌더 HTML `<head>`에 페이지별 **title/description/canonical/OG/JSON-LD 주입** (공연 = `TheaterEvent`, 공지·보도 = `NewsArticle`) — `src/ssg/prerender-seo.ts`
- `/admin`·동적 `:param` 라우트는 프리렌더 제외 (색인 대상 아님, Quill 에디터 SSR 크래시 회피)

### 기타
- 원인·해결 정리 문서 추가 및 `SEO_GUIDE.md`에 프리렌더 전제·운영 주의사항 반영
- `community/docs/` 디렉토리를 로컬 참고용으로 전환(추적 해제)

---

## v1.4.0 — 2026-07-08

UI/UX 개선 및 인프라 정리 — 로딩/에러 상태, 스크롤 리빌·푸터 개편 등 디테일 다듬기, 접근성, HTTPS 전환.

### 추가
- 리스트/관리자/배너 **API 실패 시 에러 상태 + 다시 시도** (`ErrorState`)
- **맨 위로 스크롤 버튼** (전역, 스크롤 시 노출)
- **스크롤 리빌 애니메이션** — `v-reveal` 전역 디렉티브(IntersectionObserver 공유), 홈 섹션·소개 블록·공연 카드(스태거)·목록에 적용, `prefers-reduced-motion` 존중
- 목록 페이지 **결과 카운트("총 N개") + 적용 필터·검색어 칩** 표시

### 개선
- **푸터 슬림 밴드형 재구성** — 바로가기 내비·클릭 가능한 연락처(tel/mailto)·카피라이트 추가, SNS 아이콘을 내비 줄에 통합, 반응형(태블릿 2단·모바일 압축 스택 + 구분점)
- 소개 연혁을 **세로선 + 연도 닷 타임라인**으로 개편 (창단 항목 포인트 강조)
- **활성 카테고리 필터를 색 채움**으로 선택 상태 명확화
- 공유 버튼 크기 축소(모바일 대응) 및 카테고리 칩 구조 정리
- 콘텐츠 포스터 **이미지 lazy loading** (배너·상세 대표 이미지는 LCP라 eager 유지)
- **관리자 목록 로딩을 공개 페이지와 스켈레톤 통일** (텍스트 → `ListRowSkeleton`/`ErrorState`)
- 포스터 **세로 비율 확대** (3/4 → 5/8, 목록·상세·홈 공통)
- **HTTPS 전환** (http→https 도메인 일괄), Nginx 리버스 프록시 확정
- 백엔드 패키지 매니저 **pnpm 통일**

### 수정
- 반응형: 헤더-콘텐츠 오프셋(8px 가림), 대관 이미지 여백 오타, 푸터 태블릿 주소 넘침, 소개 연혁 타임라인·환불 표 가독성
- UI 위계: 대관안내 섹션 제목 크기, 관리자 **수정/삭제 버튼 구분**(삭제=붉은색), 대관 다운로드 버튼화
- 홈 로딩 중 콘텐츠가 중앙으로 좁아지던 현상, 포스터 스켈레톤 크기 불일치
- `pinia` 미설치로 앱 부팅 실패, 컴포넌트 `name` 복붙 잔재

### 접근성
- 전역 `:focus-visible` 포커스 표시 (키보드 전용)
- 이미지 `alt` 텍스트 보완, 클릭 가능한 목록 행 **키보드 접근**(tabindex/role/Enter)

### 기타
- **백엔드 API 문서(Swagger)** 추가 — `openapi.yaml`(OpenAPI 3.0, 엔드포인트 15종) + `/api/docs` 열람 전용 UI(쓰기 API 무인증이라 "Try it out" 비활성), `koa2-swagger-ui` 도입
- SEO 문서 5종을 `SEO_GUIDE.md`로 통합, 미참조 에셋·apoc 흔적·고아 CSS 정리, `sitemap.xml` 추적 제거

---

## v1.3.0 — 2026-07-07

코드베이스 정리 및 보안 하드닝 — 데드코드/미사용 의존성 제거, 히스토리 시크릿 정리, 스키마 문서화, 스켈레톤 UI 추가.

### 추가
- 목록/상세 페이지 **스켈레톤 UI** — 데이터 로딩 중 콘텐츠 자리 표시로 체감 속도 개선

### 보안
- git 히스토리에서 유출 시크릿 완전 제거 (SSH 키·`.env`·`ecosystem.config.js`·`.idea`) — filter-repo로 재작성
- `.idea`(JetBrains, DB 연결정보 포함) 추적 해제

### 기타
- **데드코드 정리**
  - 프론트: 미사용 컴포넌트/유틸/스토어 파일, 미사용 export 12개, 미연결 API·고아 DTO, 주석 처리된 죽은 코드 제거
  - 백엔드: `types/index.ts` 이커머스 프로젝트 잔재 제거(622줄 → 46줄), 미호출 서비스 함수·죽은 DTO 필드 제거
  - 미참조 이미지 에셋 제거
- **미사용 의존성 제거**
  - 프론트: Storybook 스택 전체, `@egjs/*`(flicking), 미사용 quill 플러그인, `@vue/test-utils` 등
  - 백엔드: 중복 `multer`, `@types/dotenv`
- **문서**
  - ERD 문서 추가 (Mermaid + ERDCloud 링크) 및 README 연결
  - 아키텍처 다이어그램 문서 신설 (Mermaid)
  - `ddl.sql`을 실제 코드 스키마와 일치하도록 보정 (`board_type`, 공연 부제·카테고리 등)

---

## v1.2.0 — 2026-06-26

유지보수 스프린트 — 상세 페이지 UX·디자인 일관성, 반응형 안정화, SEO·보안·구조 개선을 묶은 정비 작업.

### 추가
- 상세 페이지(자체/대관 프로그램·공지·보도자료)에 **공유 버튼** 추가
  - Web Share API 우선 → 미지원 환경에서 링크 복사 폴백
- 상세 페이지 **동적 메타/OG/JSON-LD** 적용 및 사이트맵에 개별 글·공연 URL 포함(빌드 타임 생성)
- 관리자 목록 뷰에 **로딩/에러 상태** 표시

### 개선
- 자체/대관 프로그램 **상세 페이지 레이아웃 개편** (포스터 + 정보 패널) 및 두 페이지 통일
- **카드/태그 스타일 일관성 통일** — radius·그림자·pill 태그 (P1)
- **디자인 토큰 스케일 신설 및 점진 적용** — gray/radius/shadow/z-index/container (P2)
- 공개 상세 페이지를 쿼리스트링 → 경로 파라미터 라우팅으로 전환
- 배너 관리 페이지 및 등록/수정 팝업(모달) 디자인 개선
- 공지/보도자료 상세 가독성 개선
- `moment` → `dayjs` 교체로 번들 경량화
- 관리자 목록 공통 로직을 `useAdminList` 컴포저블로 추출

### 수정
- 모바일 레이아웃 깨짐 3건 (카테고리 필터 가로 스크롤, 포스터 오버플로, 카드 그리드 폴백) (P0)
- 모바일 공지/보도자료 상세 메타·버튼 정렬
- 기본 배너 슬라이드 배경 누락
- 미사용 변수 제거 및 모바일 터치 타깃(44px) 확보

### 보안
- 에러 응답에서 내부 메시지 노출 차단
- 자격증명을 `.env`로 이전, 카카오맵 키 환경변수 분리
- 백엔드 `tsconfig` strict 완전 적용
- 페이지네이션 상한(MAX_ROWS=100) 적용

### 기타
- 미사용·중복 의존성 제거
- 사이트 CSS 전체 점검 및 개선 (P0~P3)
- README/CHANGELOG 정비

---

## v1.1.0 — 2025-12-16

배너 관리 및 검색 노출(SEO) 강화.

### 추가
- 기본 배너 활성/비활성 관리 기능

### 개선
- SEO·nginx 설정 업데이트, OG 이미지(SNS 공유 썸네일) 추가, 고정 canonical URL 제거로 색인 개선
- 네이버 서치어드바이저 등록 및 가이드 문서 추가
- 헤더 드롭다운 메뉴 정렬, 알림/공지 메뉴 텍스트 정리, 활동 게시물 정렬 기준 변경
- 오시는 길 카카오맵 위치 수정

---

## v1.0.0 — 2025-11-05

초기 오픈 버전 — 사이트 전체 구축.

- 공연(자체/대관)·공지·보도자료 게시 및 관리자 기능
- Quill 에디터, 모바일 반응형 UI, 카카오맵(오시는 길)
- www 도메인 자동 리디렉션, 이미지·파일 업로드 크기 제한(20MB)
